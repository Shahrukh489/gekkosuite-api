{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "GekkoSuite": "Debug"
    }
  },
  "ConnectionStrings": {
    // change to non admin user to test RLS
    "Postgres": "Host=localhost;Port=5432;Database=gekkosuite;Username=postgres;Password=postgres"
  },
  "Cors": {
    // browser origins allowed to call the API; empty/absent (e.g. production) disables CORS entirely
    "AllowedOrigins": [ "http://localhost:3000", "http://localhost:5173" ]
  }
}
